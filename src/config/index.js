// const docker = require('./docker.json')
const npm = require('./npm.json')
const git = require('./git.json')
const apm = require('./apm.json')
const vscode = require('./vscode.json')

module.exports = {
  // docker,
  npm,
  git,
  apm,
  vscode,
  // system: {
  //   name: 'system',
  //   type: 'system',
  // },
}
